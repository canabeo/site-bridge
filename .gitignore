# Python client cache
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Editor swap files
*~
*.swp
*.bak

# Build artifacts
/builds/
*.zip

# Local secrets — NEVER commit:
#   - Real site secrets live in wp-config.php on each site (not in the repo)
#   - Local client config lives in ~/.sb-sites.json or wherever
#     SB_SITES_CONFIG points (not in the repo)
*.secret
*.env
.sb-sites.json
sb-sites.json
wordpress-sites.json
